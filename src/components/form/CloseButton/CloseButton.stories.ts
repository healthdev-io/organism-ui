import { CloseButton } from ".";
import { Meta, StoryObj } from "@storybook/react";

/**
 * TODO: Improve docs with better examples
 */

export default {
  title: "Form/CloseButton",
  component: CloseButton,
} as Meta;

export const Default = {} as StoryObj;
