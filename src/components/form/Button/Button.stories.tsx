import { Button } from ".";

Button.defaultProps = {
  children: "Botão",
  variant: "contained",
  color: "primary",
};

export default {
  title: "Button",
  component: Button,
};

export const Default = {};
